# Leave empty to call the API at the same origin (works with the Vite dev
# proxy, and with any production setup where the API is reverse-proxied
# under the same domain as this site). Otherwise set it to your deployed
# API's base URL, e.g. https://api.yourdomain.com
VITE_API_URL=
